import { Card } from "../../ui/Card";

export const PostsPage = ({ data = [] }) => {
    console.log(data , 'posts');
    return (
        <Card title='text'>
            test
        </Card>
    );
  };